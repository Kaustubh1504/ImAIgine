// PERSON B OWNS THIS FILE. Stub.
// The equation must stay visible at EVERY step — that is the point of the panel.
import React from 'react';

export default function ReasoningPanel({ children }) {
  return <aside className="reasoning-panel">{children}</aside>;
}
